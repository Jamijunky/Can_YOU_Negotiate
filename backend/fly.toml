app = 'can-you-negotiate-agent'
primary_region = 'sjc'

[build]
  dockerfile = 'Dockerfile'

[http_service]
  internal_port = 8081
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 0
  force_https = true

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
